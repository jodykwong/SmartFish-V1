# 代码审查问题清单

以下仅记录问题与风险点（不包含修复建议）。按严重程度排序。

## 高风险

1) **生产凭据被提交到仓库**
   - 文件：`.env.prod`（含数据库密码与 SECRET_KEY）
   - 位置：`.env.prod:5-12`
   - 风险：任何获得代码的人均可直接访问生产数据库与伪造会话，属于严重泄露。

2) **硬编码 SECRET_KEY**
   - 文件：`app.py`
   - 位置：`app.py:43`
   - 风险：所有部署共享同一 SECRET_KEY，降低会话/签名安全性。

3) **配置读取接口暴露敏感信息**
   - 文件：`app.py`
   - 位置：`app.py:107-139`, `app.py:1317-1323`
   - 风险：`/api/config` 返回 DB 密码与多项 API Key，且未见鉴权。

4) **配置写入接口可被未授权调用**
   - 文件：`app.py`
   - 位置：`app.py:1328-1347`
   - 风险：任意请求可修改 `.env` 中的数据库与 API 配置，导致后门或服务劫持。

5) **系统启动/关机接口无鉴权**
   - 文件：`app.py`
   - 位置：`app.py:1363-1427`
   - 风险：远程请求可启动/停止子进程并触发 `os._exit(0)` 关机，造成服务中断。

## 中风险

6) **SocketIO 允许任意来源跨域连接**
   - 文件：`app.py`
   - 位置：`app.py:62`
   - 风险：`cors_allowed_origins="*"` 且未见鉴权，可能被第三方站点滥用。

7) **直接提交 `.env` 文件**
   - 文件：`.env`
   - 位置：`.env:1-60`
   - 风险：虽然当前为空值，但易被误提交真实凭据，且与 `.env.example` 冗余。

8) **关机流程使用 `os._exit(0)` 强制退出**
   - 文件：`app.py`
   - 位置：`app.py:917-951`
   - 风险：可能中断正在处理的请求/日志刷新，异常退出不利于平滑收尾。

## 低风险

9) **`.env` 写入缺少严格校验**
   - 文件：`app.py`
   - 位置：`app.py:196-249`
   - 风险：允许包含特殊字符/换行的值写入 `.env`，可能导致配置解析异常或键污染。
