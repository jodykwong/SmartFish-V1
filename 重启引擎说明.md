# ⚠️ 重要：重启引擎以加载新配置

## 问题
Media Engine 仍报错 "请在您的环境变量中设置ANSPIRE_API_KEY"

## 原因
虽然我们已经修改了 `.env` 配置文件并安装了 DuckDuckGo，但 Streamlit 子应用（Media Engine、Query Engine、Insight Engine）是独立进程，它们在启动时加载配置后就缓存了。需要重启这些引擎才能加载新配置。

## ✅ 解决方案：重启所有引擎

### 步骤1：打开浏览器
访问：http://localhost:5001

### 步骤2：关闭所有引擎
1. 点击页面右上角的"**关机**"按钮
2. 确认关机
3. 等待所有引擎停止（指示灯变红）

### 步骤3：重新启动引擎
1. 点击"**⚙️ 配置**"按钮
2. 在弹窗中点击"**保存并启动系统**"按钮
3. 等待30-60秒，直到所有引擎指示灯变**绿色**

### 步骤4：验证
所有引擎启动后（指示灯都是绿色），再试一次搜索，Media Engine 将使用免费的 DuckDuckGo 引擎，不再报错。

---

## 📝 技术细节

### 已完成的配置修改
1. ✅ `.env` 中移除了无效的 API Key 占位符
2. ✅ 安装了 `duckduckgo-search` 库
3. ✅ 配置自动回退：Anspire → Bocha → **DuckDuckGo (免费)**

### 为什么需要重启引擎？
- Streamlit 应用在启动时加载配置文件
- 修改 `.env` 后不会自动重新加载
- 必须重启进程才能读取新配置

### 验证DuckDuckGo是否生效
重启后，在 Media Engine 的日志中应该看到：
```
加载 DuckDuckGoSearch Agent
```

而不是：
```
Anspire API Key未找到！
```

---

## 🚀 快捷命令（备选方案）

如果页面操作不方便，也可以用命令行：

```bash
# 方法1：使用启动脚本（推荐，自动清理）
bash start.sh  # 按 Ctrl+C 停止，然后重新运行

# 方法2：强制清理所有端口后重启
pkill -f "python app.py"
pkill -f "streamlit run"
sleep 2
bash start.sh
```

---

## ✅ 预期结果

重启引擎后：
- ✅ Media Engine 使用 DuckDuckGo（无需 API Key）
- ✅ Query Engine 使用 DuckDuckGo（无需 API Key）
- ✅ Insight Engine 正常工作
- ✅ 搜索功能正常，无错误提示

---

**按照上述步骤操作后，所有问题将彻底解决！** 🎯
