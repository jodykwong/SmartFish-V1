# 🐟 SmartFish-V1 使用指南

## ✅ 修复完成报告

**修复时间**: 2026-01-25 08:25

### 已修复的问题

#### 1. ✅ Thinking模块数据库配置问题
- **问题**: `Either 'SQLALCHEMY_DATABASE_URI' or 'SQLALCHEMY_BINDS' must be set`
- **解决方案**: 
  - 在 `app.py` 中添加了数据库URI自动配置
  - 安装了缺失的依赖包 `psycopg2-binary`
- **状态**: ⚠️ Thinking模块需要 celery 依赖（可选功能，不影响核心舆情分析）

#### 2. ✅ Web页面弹窗问题说明
- **现象**: 打开页面后没有配置弹窗
- **原因**: 这是**正常行为**！系统检测到配置已完成且之前启动过，所以不强制弹窗
- **如何手动打开配置**: 
  - 点击搜索框左侧的"⚙️ 配置"按钮
  - 或者点击页面右上角的配置按钮

#### 3. ✅ 搜索功能使用说明
- **问题**: 输入关键字后点击搜索没有反应或提示失败
- **原因**: 需要先启动子应用（Insight、Media、Query Engine）
- **正确使用流程**：见下方"完整使用流程"

---

## 🚀 完整使用流程

### 第一步：启动系统 ✅
系统已成功启动！访问地址：
- **本地**: http://127.0.0.1:5001
- **局域网**: http://192.168.8.106:5001

### 第二步：启动子应用（重要！）

在使用搜索功能之前，必须先启动分析引擎：

1. **打开浏览器访问**: http://localhost:5001

2. **点击"保存并启动系统"按钮**（首次使用）
   - 位置：配置弹窗底部的黑色按钮
   - 或者：点击页面的"⚙️ 配置"按钮，在弹窗中点击"保存并启动系统"

3. **等待子应用启动**（约30-60秒）
   - ✅ Insight Engine (端口 8501) - 私有数据分析
   - ✅ Media Engine (端口 8502) - 多模态内容分析  
   - ✅ Query Engine (端口 8503) - 全域搜索
   - ✅ Forum Engine - 智能体协作论坛
   - ✅ Report Engine - 报告生成

4. **查看启动状态**
   - 右侧控制台会显示启动日志
   - 每个引擎标签上的指示灯会从红色→黄色→绿色
   - 全部变绿后即可使用

### 第三步：开始分析

1. **在搜索框输入分析主题**，例如：
   ```
   分析"华为Mate70"在社交媒体的舆情趋势
   ```

2. **点击"搜索"按钮**
   - 系统会自动分配任务给3个引擎
   - 可以在控制台切换标签查看各引擎的工作进度

3. **查看分析结果**
   - 左侧嵌入页面会显示各引擎的分析结果
   - 点击顶部标签可切换不同引擎的视图
   - Forum标签会显示智能体之间的讨论

4. **生成综合报告**
   - 等待3个引擎完成分析
   - Report Engine会自动生成综合报告
   - 点击"Report"标签查看最终报告

---

## 🎯 快速测试

### 测试1：验证系统启动
```bash
# 访问健康检查端点
curl http://localhost:5001/health

# 预期输出：
{"status":"ok"}
```

### 测试2：验证子应用状态
打开浏览器访问：http://localhost:5001/api/status

预期看到各应用的状态（启动后应该是 "running"）

### 测试3：简单搜索测试
1. 确保所有引擎启动完成（绿色指示灯）
2. 输入：`测试舆情分析`
3. 点击搜索
4. 观察控制台输出

---

## ⚠️ 常见问题

### Q1: 点击搜索提示"没有运行中的应用"
**A**: 需要先点击"保存并启动系统"按钮启动子应用

### Q2: 子应用启动很慢或失败
**A**: 
- 检查端口是否被占用：`lsof -i :8501,8502,8503`
- 查看控制台错误日志
- 重启系统：点击页面右上角"关机"按钮，然后重新启动

### Q3: Thinking模块错误（celery）
**A**: 
- 这是可选功能，不影响核心舆情分析
- 如需使用，安装依赖：`pip install celery redis`

### Q4: 页面打不开或显示异常
**A**:
- 清除浏览器缓存
- 使用无痕模式访问
- 检查是否有其他进程占用5001端口

### Q5: API配额不足
**A**:
- OpenRouter免费模型限制：50次/天，20次/分钟
- 每次完整报告需要约22次API调用
- 查看配额优化：`python optimize_api_quota.py show`

---

## 📊 系统架构

```
SmartFish-V1 (主应用 :5001)
├── Insight Engine (:8501) - 私有数据挖掘
├── Media Engine (:8502) - 多模态分析
├── Query Engine (:8503) - 全域搜索
├── Forum Engine - 智能体协作
└── Report Engine - 报告生成
```

---

## 🔧 管理命令

### 启动系统
```bash
bash start.sh
# 或
source venv/bin/activate
python app.py
```

### 停止系统
- 方法1：页面右上角"关机"按钮
- 方法2：终端按 `Ctrl+C`
- 方法3：`pkill -f "python app.py"`

### 查看日志
```bash
# 实时查看主应用日志
tail -f logs/app.log

# 查看特定引擎日志
tail -f logs/insight.log
tail -f logs/media.log
tail -f logs/query.log
```

### 检查配置
```bash
python check_status.py    # 快速检查
python check_config.py    # 详细检查
```

---

## 📝 配置说明

### 环境变量文件：`.env`
- **数据库配置**: 已配置 PostgreSQL
- **LLM配置**: 已使用 OpenRouter 统一模式
- **API密钥**: 已配置7个免费引擎

### 修改配置
1. 直接编辑 `.env` 文件
2. 或在Web界面点击"⚙️ 配置"按钮修改
3. 修改后需要重启系统生效

---

## 🎉 开始使用

1. ✅ 系统已启动
2. 📖 阅读完本指南
3. 🌐 打开 http://localhost:5001
4. 🚀 点击"保存并启动系统"
5. ⏳ 等待引擎启动完成
6. 🔍 输入关键字开始分析！

---

**祝您使用愉快！** 🐟✨
